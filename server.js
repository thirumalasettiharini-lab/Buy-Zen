const express=require("express");const cors=require("cors");const dotenv=require("dotenv");dotenv.config();const app=express();app.use(cors({origin:process.env.CLIENT_ORIGIN||"http://localhost:5173"}));app.use(express.json({limit:"100kb"}));app.get("/",(_,res)=>res.json({message:"BuyZen Agentic Commerce API is running"}));app.use("/api/catalog",require("./routes/CatalogRoutes"));app.use("/api/ai",require("./routes/AIRoutes"));app.use("/api/payments",require("./routes/PaymentRoutes"));app.use("/api",require("./routes/AuditRoutes"));const PORT=process.env.PORT||5000;app.listen(PORT,()=>console.log(`BuyZen server running on http://localhost:${PORT}`));
